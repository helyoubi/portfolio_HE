{
  "name": "portfolio-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js",
    "update-cache": "node update-cache-version.js",
    "deploy": "npm run update-cache && echo 'Cache updated! Now commit and push to GitHub Pages'"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^30.0.4"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": [
      "**/tests/**/*.mjs"
    ]
  }
}
